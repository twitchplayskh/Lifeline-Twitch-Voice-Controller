# All of these are optional — the app runs without them, with the
# corresponding feature (audio playback / TTS / virtual gamepad / profanity
# filter) simply disabled.

sounddevice
soundfile
numpy
pyttsx3
vgamepad      # Windows only; also requires the ViGEmBus driver
better-profanity
